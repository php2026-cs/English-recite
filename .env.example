# 当前默认词典来源（dictionaryapi.dev + MyMemory）不需要 API Key。
# 如果未来切换到需要鉴权的词典服务，可在这里填写，并确保不要提交真实 Key。
VITE_DICTIONARY_API_KEY=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_VAPID_PUBLIC_KEY=
# GitHub Pages builds set these through the workflow; local and Sites use defaults.
VITE_BASE_PATH=/
VITE_ROUTER_MODE=browser
